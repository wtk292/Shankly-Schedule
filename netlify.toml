[build]
  base = "shankly-schedule"
  command = "npm install && npm run build"
  publish = "dist"
  functions = "netlify/functions"

[build.environment]
  NODE_VERSION = "18"

[functions."daily-reminder"]
  schedule = "0 20 * * *"
